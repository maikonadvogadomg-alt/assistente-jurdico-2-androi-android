import type { CapacitorConfig } from '@capacitor/cli';
const config: CapacitorConfig = {
  appId: 'com.github.assistentejurdico2androi',
  appName: 'assistente-jurdico-2-androi',
  webDir: 'dist',
  server: { androidScheme: 'https' },
};
export default config;
